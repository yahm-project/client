<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="it.unibo.yahm">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:fontFamily="@font/beauty_and_beast"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <meta-data
            android:name="mas2j"
            android:value="/project.mas2j" />

        <service android:name="it.unibo.pslab.jaca_android.MasService" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".client.activities.MapsActivity"
            android:label="@string/title_activity_maps"
            android:screenOrientation="nosensor" />
        <activity
            android:name=".client.activities.SimulationActivity"
            android:label="@string/title_activity_simulation"
            android:screenOrientation="nosensor" />
        <activity
            android:name=".client.activities.MainActivity"
            android:label="[YAHM]"
            android:screenOrientation="nosensor">
        </activity>
        <activity
            android:name=".client.activities.TrainingActivity"
            android:label="@string/title_activity_training"
            android:screenOrientation="nosensor" />

        <activity
            android:name="it.unibo.pslab.jaca_android.LauncherActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".client.artifacts.NavigatorGUIArtifact$NavigatorActivity"
            android:launchMode="singleTask">
        </activity>

    </application>

</manifest>